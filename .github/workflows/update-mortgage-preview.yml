name: Mortgage sharing preview (static)

# This workflow replaces the former scheduled rate-image updater.
# No daily job or rate-fetch script runs after this file is uploaded.
on:
  workflow_dispatch:

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The mortgage sharing preview is now a static brand image."
